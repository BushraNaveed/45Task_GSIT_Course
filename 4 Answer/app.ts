function printQuote(quote: string, author: string) {
    console.log(`${author} once said, "${quote}"`);
}

let quote = "A person who never made a mistake never tried anything new.";
let author = "Albert Einstein";

printQuote(quote, author);
